version: '3.8'

services:
  micro-cti:
    image: esoadamo/micro-cti
    build:
      context: .
    ports:
      - "8080:80"
    volumes:
      - ./data.sqlite3:/app/data.sqlite3 
      - ./config.toml:/app/config.toml:ro
